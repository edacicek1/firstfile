#include <stdio.h>

int factorial(int n) {
    if (n == 0)
        return 1;
    else
        return n * factorial(n - 1);
}

int main() {
    int num;
    printf("Bir sayÄ± girin: ");
    scanf("%d", &num);

    if (num < 0)
        printf("Negatif sayÄ±larÄ±n faktÃ¶riyeli hesaplanamaz.\n");
    else
        printf("%d sayÄ±sÄ±nÄ±n faktÃ¶riyeli: %d\n", num, factorial(num));

    return 0;
}
